This repository follows the shared instructions in the canonical file:

`../outworld-nixos-configs/AGENTS.md`

Read that file before changing this checkout. Its package-repository scope
covers `flake.nix`, `packages/<name>/default.nix`, package independence,
export compatibility, validation, and secret handling.
